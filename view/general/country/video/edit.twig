{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-%s")|format(message.country)) %}
{% do headTitle().append(translate("txt-edit-video")) %}

<h1>{{ translate("txt-edit-video-of-%s")|format(video.country) }}</h1>

{% include 'general/partial/form/country/video' %}