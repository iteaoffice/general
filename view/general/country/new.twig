{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-new-country")) %}

<h1 class="display-4">{{ translate("txt-new-country") }}</h1>

{% include 'general/partial/form/country' %}
