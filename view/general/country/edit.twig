{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-country")) %}

<h1>{{ translate("txt-edit-country") }}</h1>

{% include 'general/partial/form/country' %}