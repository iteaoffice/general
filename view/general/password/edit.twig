{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-password")) %}

<h1>{{ translate("txt-edit-password") }}</h1>

{% include 'general/partial/form/password' %}