{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-new-password")) %}

<h1>{{ translate("txt-new-password") }}</h1>

{% include 'general/partial/form/password' %}
