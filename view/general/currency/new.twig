{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-new-currency")) %}

<h1>{{ translate("txt-new-currency") }}</h1>

{% include 'general/partial/form/currency' %}
