{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-new-content-type")) %}

<h1>{{ translate("txt-new-content-type") }}</h1>

{% include 'general/partial/form/content-type' %}
