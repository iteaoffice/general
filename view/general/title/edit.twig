{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-title")) %}

<h1 class="display-4">{{ translate("txt-edit-title") }}</h1>

{% include 'general/partial/form/title' %}