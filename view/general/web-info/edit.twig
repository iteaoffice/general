{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-web-info")) %}

<h1>{{ translate("txt-edit-web-info") }}</h1>

{% include 'admin/partial/form/web-info' %}