#!/usr/bin/env sh
SRC_DIR="`pwd`"
cd "`dirname "$0"`"
cd ".."

git push origin develop --tags

REV="`git rev-parse HEAD`"
php ocular.phar code-coverage:upload --format=php-clover --repository=g/iteaoffice/general --revision="$REV" clover.xml
